server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kankan?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: kankan
    password: "Pangoni0425!"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
